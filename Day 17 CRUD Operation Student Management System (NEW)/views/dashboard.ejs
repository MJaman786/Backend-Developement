<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard (Combined View)</title>
    <style>
        :root {
            /* "Matte Dark" Palette */
            --bg-page: #121212;
            --bg-card: #1c1c1c;
            --bg-secondary: #252525;
            --bg-hover: #2a2a2a;
            --border-default: #333333;
            --border-focus: #666666;

            --text-main: #e5e5e5;
            --text-muted: #a1a1aa;

            /* Action Colors */
            --action-edit: #4A90E2;
            /* Muted Blue */
            --action-delete: #E24A4A;
            /* Muted Red */
            --action-add: #28a745;
            /* Muted Green */

            --spacing-xs: 8px;
            --spacing-md: 16px;
            --spacing-lg: 32px;

            --radius: 4px;
        }

        html {
            color-scheme: dark;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg-page);
            color: var(--text-main);
            min-height: 100vh;
            padding: var(--spacing-lg) var(--spacing-md);
        }

        .dashboard-container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
        }

        /* --- Header and Toolbar --- */
        .header-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: var(--spacing-lg);
            padding: 0 0 10px 0;
            border-bottom: 1px solid var(--border-default);
        }

        h1 {
            font-size: 1.8rem;
            font-weight: 500;
        }

        /* --- Button Styling --- */
        .btn {
            padding: 10px 20px;
            border-radius: var(--radius);
            font-weight: 600;
            font-size: 0.9rem;
            cursor: pointer;
            text-decoration: none;
            transition: opacity 0.2s ease, background-color 0.2s ease;
            text-align: center;
            border: none;
        }

        .btn-add {
            background-color: var(--action-add);
            color: var(--bg-card);
        }

        .btn-add:hover {
            opacity: 0.9;
        }

        /* List Action Buttons (Inline) */
        .btn-action {
            padding: 6px 10px;
            border: 1px solid var(--border-default);
            margin-left: 8px;
            background-color: var(--bg-secondary);
            color: var(--text-main);
        }

        .btn-action:hover {
            opacity: 0.85;
        }

        .btn-edit {
            color: var(--action-edit);
            border-color: var(--action-edit);
        }

        .btn-delete {
            color: var(--action-delete);
            border-color: var(--action-delete);
        }


        /* --- Table Styling for Horizontal Scroll --- */

        /* The Wrapper enables horizontal scrolling */
        .table-scroll-wrapper {
            overflow-x: auto;
            width: 100%;
            /* Styling the wrapper to look like the main data block */
            background-color: var(--bg-card);
            border: 1px solid var(--border-default);
            border-radius: var(--radius);
            /* This section is intentionally not hidden with CSS */
        }

        /* The Table itself */
        .student-table {
            width: 100%;
            border-collapse: collapse;
            min-width: 900px;
        }

        /* Table Header and Cells */
        .student-table th,
        .student-table td {
            padding: 12px 16px;
            text-align: left;
            border-bottom: 1px solid var(--border-default);
            white-space: nowrap;
        }

        .student-table th {
            background-color: #242424;
            color: var(--text-muted);
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-weight: 600;
            position: sticky;
            top: 0;
        }

        .student-table tbody tr:hover {
            background-color: var(--bg-hover);
        }

        .student-table tbody tr:last-child td {
            border-bottom: none;
        }

        /* Specific column alignments */
        .col-id {
            color: var(--text-muted);
            width: 80px;
        }

        .col-actions {
            text-align: right;
            width: 180px;
        }

        .col-name {
            font-weight: 500;
        }

        /* Custom scrollbar styling */
        .table-scroll-wrapper::-webkit-scrollbar {
            height: 8px;
        }

        .table-scroll-wrapper::-webkit-scrollbar-thumb {
            background: #555;
            border-radius: 4px;
        }

        .table-scroll-wrapper::-webkit-scrollbar-track {
            background: #2b2b2b;
        }


        /* --- No Data Message Styling --- */
        .no-data-message {
            background-color: var(--bg-card);
            padding: 60px 20px;
            border: 1px dashed var(--border-focus);
            border-radius: var(--radius);
            text-align: center;
            margin-top: 30px;
            /* This section is intentionally not hidden with CSS */
        }

        .no-data-message h2 {
            font-size: 1.5rem;
            font-weight: 500;
            color: var(--text-main);
            margin-bottom: 10px;
        }

        .no-data-message p {
            font-size: 1rem;
            color: var(--text-muted);
            margin-bottom: 25px;
        }

        .no-data-message .btn-add {
            padding: 12px 30px;
            font-size: 1rem;
        }
    </style>
</head>

<body>

    <div class="dashboard-container">

        <div class="header-section">
            <h1>Student Roster</h1>
            <!-- The 'Add New Student' button remains visible -->
            <a href="/registration_form" class="btn btn-add">
                <!-- Inline SVG for minimal look -->
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16"
                    style="vertical-align: -2px; margin-right: 6px;">
                    <path
                        d="M8 0a1 1 0 0 1 1 1v6h6a1 1 0 1 1 0 2H9v6a1 1 0 1 1-2 0V9H1a1 1 0 0 1 0-2h6V1a1 1 0 0 1 1-1z" />
                </svg>
                Add New Student
            </a>
        </div>
        <!-- ============================================== -->
        <!-- DATA TABLE CONTAINER - Hide/Show with JavaScript -->
        <!-- ============================================== -->
        <%if (user !==undefined && user.length> 0) {%>
            <div class="table-scroll-wrapper" id="studentTableWrapper">
                <table class="student-table">
                    <thead>
                        <tr>
                            <th class="col-id">ID</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Course</th>
                            <th>Phone</th>
                            <th>Date</th>
                            <th class="col-actions">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <%user.forEach((user, index)=>{%>
                            <tr>
                                <td class="col-id"><%- index+1%></td>
                                <td class="col-name"><%- user.fullname%></td>
                                <td><%- user.email%></td>
                                <td><%- user.course%></td>
                                <td><%- user.phone%></td>
                                <td><%- user.createdAt%></td>
                                <td class="col-actions">
                                    <a href="/editUser/<%- user._id%>" class="btn btn-action btn-edit">Edit</a>
                                    <a href="/deleteUser/<%- user._id%>" class="btn btn-action btn-delete">DELETE</a>
                                </td>
                            </tr>
                        <%});%>
                    </tbody>
                </table>
            </div>
            <!-- ============================================== -->
            <!-- NO DATA FOUND CONTAINER - Hide/Show with JavaScript -->
            <!-- ============================================== -->
            <%} else {%>
                <div class="no-data-message" id="noDataMessage">
                    <h2>No Student Data Found</h2>
                    <p>It looks like there are no students registered yet. Click the button below to get started.</p>
                    <a href="/registration_form" class="btn btn-add">
                        Start Registering Students
                    </a>
                </div>
            <%}%>
    </div>
    <script>

    </script>
    <!-- Student Row 1 -->

</body>

</html>