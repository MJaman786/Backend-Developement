<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Student Profile</title>
    <style>
        :root {
            /* "Matte Dark" Palette */
            --bg-page: #121212;
            /* Very dark grey, almost black */
            --bg-card: #1c1c1c;
            /* Slightly lighter panel */
            --bg-input: #252525;
            /* Input background */
            --border-default: #333333;
            --border-focus: #666666;
            /* Subtle highlight */

            --text-main: #e5e5e5;
            /* Off-white for readability */
            --text-muted: #a1a1aa;
            /* Grey for labels */
            --accent: #f8f8f8;
            /* High contrast action button */

            --spacing-xs: 8px;
            --spacing-md: 16px;
            --spacing-lg: 32px;

            --radius: 4px;
        }

        /* Force browser native elements (date picker, scrollbars) to dark mode */
        html {
            color-scheme: dark;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg-page);
            color: var(--text-main);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: var(--spacing-md);
        }

        .registration-container {
            width: 100%;
            max-width: 500px;
            background-color: var(--bg-card);
            padding: 40px;
            border-radius: var(--radius);
            border: 1px solid var(--border-default);
        }

        header {
            margin-bottom: 30px;
        }

        h1 {
            font-size: 1.5rem;
            font-weight: 500;
            letter-spacing: -0.5px;
            color: var(--text-main);
            margin-bottom: var(--spacing-xs);
        }

        p.subtitle {
            color: var(--text-muted);
            font-size: 0.875rem;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        label {
            display: block;
            color: var(--text-muted);
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
            font-weight: 600;
        }

        input,
        select,
        textarea {
            width: 100%;
            padding: 12px 16px;
            background-color: var(--bg-input);
            border: 1px solid var(--border-default);
            border-radius: var(--radius);
            color: var(--text-main);
            font-size: 0.95rem;
            font-family: inherit;
            transition: border-color 0.2s ease, background-color 0.2s ease;
            outline: none;
        }

        /* Remove default arrow for select to style it custom if needed, 
           but for pure accessibility/native dark mode we keep native but styled */
        select {
            appearance: none;
            background-image: url("data:image/svg+xml;charset=US-ASCII,%3Csvg%20xmlns%3D%22http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%22%20width%3D%22292.4%22%20height%3D%22292.4%22%3E%3Cpath%20fill%3D%22%23a1a1aa%22%20d%3D%22M287%2069.4a17.6%2017.6%200%200%200-13-5.4H18.4c-5%200-9.3%201.8-12.9%205.4A17.6%2017.6%200%200%200%200%2082.2c0%205%201.8%209.3%205.4%2012.9l128%20127.9c3.6%203.6%207.8%205.4%2012.8%205.4s9.2-1.8%2012.8-5.4L287%2095c3.5-3.5%205.4-7.8%205.4-12.8%200-5-1.9-9.2-5.5-12.8z%22%2F%3E%3C%2Fsvg%3E");
            background-repeat: no-repeat;
            background-position: right 1rem center;
            background-size: 0.65em auto;
            padding-right: 2.5rem;
        }

        textarea {
            resize: vertical;
            min-height: 80px;
        }

        /* Interaction States - No Glow, just Matte changes */
        input:focus,
        select:focus,
        textarea:focus {
            border-color: var(--border-focus);
            background-color: #2a2a2a;
        }

        input::placeholder,
        textarea::placeholder {
            color: #555;
        }

        /* Button Styling */
        .btn-submit {
            width: 100%;
            padding: 14px;
            background-color: var(--text-main);
            color: var(--bg-page);
            border: none;
            border-radius: var(--radius);
            font-weight: 600;
            font-size: 0.95rem;
            cursor: pointer;
            margin-top: 10px;
            transition: opacity 0.2s ease;
        }

        .btn-submit:hover {
            opacity: 0.9;
        }

        /* Mobile Responsiveness */
        @media (max-width: 600px) {
            .registration-container {
                padding: 25px;
                border: none;
                background-color: transparent;
            }

            body {
                align-items: flex-start;
                padding: 0;
            }

            .form-row {
                grid-template-columns: 1fr;
                gap: 0;
            }

            input,
            select,
            textarea {
                padding: 14px 16px;
                /* Larger touch targets */
            }
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="registration-container">
            <header>
                <h1>Edit Student Profile</h1>
                <p class="subtitle">Update your personal details and course enrollment information.</p>
            </header>

            <form action="/updateStudent/<%-user._id%>" method="POST">
                <!-- Full Name -->
                <div class="form-group">
                    <label for="fullname">Full Name</label>
                    <input type="text" id="fullname" name="fullname" value="<%- user.fullname%>" required>
                </div>

                <!-- Email & Phone Row -->
                <div class="form-row">
                    <div class="form-group">
                        <label for="email">Email Address</label>
                        <input type="email" id="email" name="email" value="<%- user.email%>" required>
                    </div>
                    <div class="form-group">
                        <label for="phone">Phone Number</label>
                        <input type="tel" id="phone" name="phone" value="<%- user.phone%>">
                    </div>
                </div>

                <!-- Course Selection -->
                <div class="form-group">
                    <label for="course">Select Course</label>
                    <select id="course" name="course" required>
                        <option value="" disabled>Select a program...</option>

                        <option value="cs" <%=user.course==="cs" ? "selected" : "" %>>
                            Computer Science
                        </option>

                        <option value="ds" <%=user.course==="ds" ? "selected" : "" %>>
                            Data Science
                        </option>

                        <option value="wd" <%=user.course==="wd" ? "selected" : "" %>>
                            Web Development
                        </option>

                        <option value="ui" <%=user.course==="ui" ? "selected" : "" %>>
                            UI/UX Design
                        </option>

                        <option value="mk" <%=user.course==="mk" ? "selected" : "" %>>
                            Digital Marketing
                        </option>
                    </select>

                </div>

                <!-- DOB & Gender Row -->
                <div class="form-row">
                    <div class="form-group">
                        <label for="dob">Date of Birth</label>
                        <input type="date" id="dob" name="dob" value="<%- user.dob%>">
                    </div>
                    <div class="form-group">
                        <label for="gender">Gender</label>
                        <select id="gender" name="gender">
                            <option value="" disabled>Select...</option>

                            <option value="male" <%=user.gender==="male" ? "selected" : "" %>>
                                Male
                            </option>

                            <option value="female" <%=user.gender==="female" ? "selected" : "" %>>
                                Female
                            </option>

                            <option value="other" <%=user.gender==="other" ? "selected" : "" %>>
                                Non-binary / Other
                            </option>

                            <option value="prefer-not" <%=user.gender==="prefer-not" ? "selected" : "" %>>
                                Prefer Not to Say
                            </option>
                        </select>

                    </div>
                </div>

                <!-- Address -->
                <div class="form-group">
                    <label for="address">Permanent Address</label>
                    <textarea id="address" name="address"
                        placeholder="Street, City, Zip Code..."><%- user.address%></textarea>
                </div>

                <!-- Submit -->
                <button type="submit" class="btn-submit">Save Changes</button>
            </form>
        </div>
    </div>

</body>

</html>