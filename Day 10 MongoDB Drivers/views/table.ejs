<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Data</title>
    <link rel="stylesheet" href="/styles/table.css">
</head>
<body>

    <div class="table-container">
        <h2>Registered Users</h2>
        
        <!-- 
          This EJS code checks if the 'users' array exists and has items.
          If it does, it creates a table and loops over each user.
          If not, it displays a "No users found" message.
        -->
        
        <% if (typeof users !== 'undefined' && users.length > 0) { %>
            <table>
                <thead>
                    <tr>
                        <th>Username</th>
                        <th>Email</th>
                        <!-- 
                          SECURITY NOTE: 
                          You should NEVER display passwords in a table, even for practice.
                          I have omitted the password column intentionally.
                        -->
                    </tr>
                </thead>
                <tbody>
                    <!-- Loop over each user in the 'users' array -->
                    <% users.forEach(function(user) { %>
                        <tr>
                            <td><%= user.username %></td>
                            <td><%= user.email %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else { %>
            <p class="no-users">No users found in the database.</p>
        <% } %>

    </div>

</body>
</html>
